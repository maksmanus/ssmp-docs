const _https = "https://"

function SetHomeURL()
{
    if(window.location.hostname("github.io"))
    {
        let hostname = window.location.hostname
        let link = document.getElementById("chaeckGit");
        link.setAttribute("href", _https + window.location.)
    }
}